fn main() { /* main fn always execute first, it should exists in every rust program */
    println!("Hello World!");
}

// ! it's to indicates is a macro not a function.
// if you want to call a function, you should not use the !